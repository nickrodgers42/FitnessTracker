import {
    Dimensions,
    StyleSheet,
} from 'react-native';

export default StyleSheet.create({
    titleText: {
        fontSize: 24,
        fontWeight: 'bold',
        paddingRight: 20,
        paddingTop: 20,
    },
    bigText: {
        fontSize: 40, 
        textAlign: 'center'
    },
    centerItems: {
        justifyContent: 'center',
        alignItems: 'center'
    }
});
